// copy right (c) Mainul Iftekher

/*namespace AdventureGame
{
    class Game
    {
        
        public const int MAP_SIZE = 5;
        // local variables
        private Location _currentLocation = new Location("Start");
        public Game()
        {
            //create ,ap
            // load locations. location data
            // setup game


        }
        
        public void Run()
        {
            Render();
            while(!gameOver)
            {

                Update();
                Render();
            }

        }

        private void Update()
        {
            // collect user input
            //update _currentLocation
            // update player state
        }

        private void Render()
        {
            // use the _currentLocation to show description
            // prompt user for new actions
        }
    }

*/